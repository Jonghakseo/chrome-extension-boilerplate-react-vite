{
  "name": "@extension/messaging",
  "version": "0.3.1",
  "description": "chrome extension - messaging api",
  "private": true,
  "sideEffects": false,
  "files": [
    "dist/**"
  ],
  "types": "./index.ts",
  "main": "./dist/index.js",
  "scripts": {
    "clean": "rimraf ./dist",
    "ready:once": "node build.mjs",
    "lint": "eslint . --ext .ts,.tsx",
    "lint:fix": "pnpm lint --fix",
    "prettier": "prettier . --write --ignore-path ../../.prettierignore",
    "type-check": "tsc --noEmit"
  },
  "dependencies": {},
  "devDependencies": {
    "@extension/tsconfig": "workspace:*"
  }
}
